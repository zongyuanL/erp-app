server:
  port: 9999
spring:
  application:
    name: gateway-service
  cloud:
    config:
      name: gateway-service,basicConfig  #文件前缀名称
#      profile: dev
#      uri: http://localhost:8088
      username: alex
      password: 123456
      discovery:
        enabled: true
        service-id: config-server

